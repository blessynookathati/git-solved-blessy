application:
  name: DevOpsSimulator
  version: 1.0.0
  environment: production

server:
  host: 0.0.0.0
  port: 8080
  ssl_enabled: true

logging:
  level: INFO
  format: json

# --- EXPERIMENTAL AI/ML CONFIGURATION ---
experimental:
  ai_enabled: false
  model_path: /models/v3.0/
  gpu_required: false
  endpoint: /api/v3/predict